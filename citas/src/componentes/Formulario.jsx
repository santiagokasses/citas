import React, { Fragment } from "react"
const Formulario = (props) => {
    return(
        <Fragment>
            <label>{props.formulario}</label>
        </Fragment>
    )
}

export default Formulario